<%=
  Rails.logger.info('content context for part title edit: ' + content.inspect)
  render "govuk_publishing_components/components/input", {
  label: {
    text: "Part Title",
    bold: true
  },
  id: input_context.id("part_title"),
  name: input_context.name("part_title"),
  value: content.to_s
} %>
