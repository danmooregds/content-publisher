Parts start

<% field.contents.each do |subfield| %>
  <%= render "content/edit/#{subfield.id}_field",
             edition: @edition,
             issues: @issues,
             field: subfield,
             input_context: DocumentType::InputContext.new([:parts, 0]),
             content: field.subfield_content(content, subfield) %>
<% end %>

Parts depart
