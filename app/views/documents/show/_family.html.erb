<%= render "govuk_publishing_components/components/summary_list", {
  id: "family",
  title: "Family",
  borderless: true,
  items: [
          {
            field: 'parent',
            value: (@edition.has_parent? ? "#{@edition.parent.title} (of #{@edition.parents.length})" : 'no parent')
          },
          {
            field: 'children',
            value: (@edition.has_children? ? @edition.children.map(&:current_edition).map(&:title) : 'no children')
          }
  ]
} %>
